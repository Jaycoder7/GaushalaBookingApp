version: '3.9'

services:
  postgres:
    image: postgres:14-alpine
    container_name: gaushala-postgres
    environment:
      POSTGRES_DB: gaushala_dev
      POSTGRES_USER: gaushala
      POSTGRES_PASSWORD: gaushala_dev_password
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U gaushala']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
